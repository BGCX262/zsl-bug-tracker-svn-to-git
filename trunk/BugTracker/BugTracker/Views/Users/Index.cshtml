@model IEnumerable<BugTracker.ViewModels.UsersViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>@Html.Label(item.Users.Username)</td>
    </tr>
    @*<tr>
        <td>@Html.Display(item.User.Password)</td>
    </tr>
    <tr>
        <td>@Html.Display(item.User.ModifiedDate.ToString())</td>
    </tr>
    <tr>
        <td>@Html.Display(item.User.IsZsl.ToString())</td>
    </tr>
    <tr>
        <td>@Html.Display(item.User.IsActive.ToString())</td>
    </tr>
    <tr>
        <td>@Html.Display(item.User.Email.ToString())</td>
    </tr>*@
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
